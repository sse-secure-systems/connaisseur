apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-${index}
  labels:
    use: integration-test
spec:
  selector:
    matchLabels:
      use: integration-test
  replicas: 1
  template:
    metadata:
      labels:
        use: integration-test
    spec:
      containers:
      - name: redis
        image: redis
